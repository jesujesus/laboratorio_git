# laboratorio_git
# Guía rápida de Git 🧠💻

> Archivo: `README.md`  
> Úsalo como chuleta básica para trabajar con Git y GitHub.

---

## 1. Configuración inicial

Estos comandos se usan **solo la primera vez** en tu computadora (o cuando cambies de usuario).

```bash
# Configurar nombre de usuario (aparece en los commits)
git config --global user.name "tu_usuario"

# Configurar correo (debe ser el mismo de tu cuenta de GitHub si quieres asociar los commits)
git config --global user.email "tu_correo@ejemplo.com"

# Ver configuración guardada
git config --list
